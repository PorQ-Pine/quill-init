import { Button } from "button.slint";
export component Dialog inherits Rectangle {
    in property <string> text <=> i-text.text;
    property <length> button_width: root.width * 0.4;
    property <length> button_height: root.height * 0.15;
    callback cancel();
    callback confirm();

    border-color: black;
    border-width: 6px;
    background: white;

    TouchArea {
        width: parent.width;
        height: parent.height;
        enabled: true;
    }
    VerticalLayout {
        padding: 40px;
        Rectangle {}
        i-text := Text {
            horizontal-alignment: center;
            wrap: word-wrap;
        }
        Rectangle {}
        HorizontalLayout {
            spacing: 30px;
            alignment: center;
            Button {
                text: "Cancel";
                width: button_width;
                height: button_height;
                border-radius: root.border-radius;
                clicked => {
                    cancel();
                }
            }
            Button {
                text: "Confirm";
                width: button_width;
                height: button_height;
                border-radius: root.border-radius;
                clicked => {
                    confirm();
                }
            }
        }
    }
}
